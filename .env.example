# Environment Variables Example
# Copy this file to .env.local for local development

# Backend API URL (optional - auto-detected based on hostname)
# VITE_API_URL=http://localhost:8000

# The application will automatically use:
# - http://localhost:8000 when running on localhost
# - https://wdmniyiwug.us-east-1.awsapprunner.com when deployed on Amplify
# - VITE_API_URL if set (overrides auto-detection)
